# Personal runtime data — never commit.
projects.yaml
*.log
*.err
*.out

# Local working TODO lists — kept in docs/dev/ but not versioned.
docs/dev/*.md

# OS / editor noise
.DS_Store
*.swp

# Ephemeral git worktrees from bridge sessions
.claude/worktrees/

# Local user settings — not versioned.
.claude/settings.local.json
